# Sun glow separation and pause-menu panel

## Run evidence

PID 31848, built September 17 at 11:42:07. Preserved log/captures/shaders in `.analysis/sun-menu-20260917/`.

- Marker 1: frame 4683, capture 4686, 412 draws / 12 pages, no truncation.
- Marker 2: frame 10319, capture 10322, 306 draws / 8 pages, no truncation.
- Marker 3: frame 14830, capture 14833, 2048 draws / 34 pages, truncated during repeated world draws before the pause-menu passes.

The GPU thumbnail capture and page saves succeeded in the actual game. Ordered before/after images attribute the displaced bright sun copy to **VS E6318E88 / PS A1D87F1F**, draw 339 in capture 4686 and draw 234 in capture 10322. The first sun marker already has a distant sky sun from DF014469 / 2A5A0149 at draw 319 before this extra glow is added. The old 1612477A / 2C531FCD diagnostic pair changes no pixels at the marked spot.

E6318E88 constructs camera-facing billboards from CameraRight/CameraUp and projects them through c0..3. A1D87F1F samples s0, using rotated texture UVs and per-vertex glow parameters. Both the primary sun and this glow bind the same 512x512 starburst texture. This is a separate billboard contribution, not the glass building's material shader. The user's flat-game photo shows one combined glow above the glass building; the VR capture has an extra separated bright spot at inconsistent positions between eyes. The sprite's exact vertex positions were not in the previous vertex capture, so its distance has not yet been measured directly.

The user confirmed the pause-menu problem is the red panel across the middle, not the text. Selective full-size captures show text duplicated per eye while the red panel spans the eye boundary. They do not isolate the draw that introduced the red panel: the broad trace stopped at repeated world decals before reaching the menu. No speculative menu-rendering fix is applied.

## Changes

- Apply the existing distant-sun camera-anchored stereo correction to the **exact observed E6318E88/A1D87F1F pair**. Both eyes retain head rotation/FOV, but use the billboard's original CameraPosition for projection rather than separate translated cameras. This is intended to reunite the glow with the sun, not remove it. Other E6318E88 materials keep ordinary positional stereo.
- Keep the earlier 1612477A/2C531FCD route unchanged; no global shader suppression or reflection-material change.
- Capture the actual glow's UP vertices/texture/constants and full-size paired images on the next Backspace marker for verification.
- Reserve up to 256 additional depth-disabled, non-world-projection draws after the 2048 general-draw budget. Late pause-menu draws can now be captured even after world geometry exhausts that budget. Allocation failure still stops capture; frame/present/reset cleanup is preserved.
- Existing LensFlares off / NativeLensFlareSuppression off settings remain unchanged.

## Validation

Production haze/sun tests passed for both exact shader pairs, retained rotation/FOV, removal of positional eye disparity, success/failure state restoration, and exclusion of other billboards. Existing haze and shadow reconstruction tests passed. Capture tests passed for the additional reserved budget, ordering, failures, cleanup and existing full-size captures. Mandatory static analysis/release build passed with existing advisory warnings.

Installed build SHA256: `1B272AADEF7CBAFCF8255166565FB2C1A5FECB2674FB99E6ECFB7BBB45D4466F`.

The sun patch still needs headset confirmation. The red-panel issue remains unresolved; a pause-menu Backspace capture with the reserved late-draw budget is needed to isolate its producer. No need to retest the menu text separately.
