# Ground pickup and catches, September 17

User confirmed sun and pause menu now work. This change concerns pistol selection,
highlighting and catches; no rendering classification or INI changes.

## Evidence

Run 22264, saved in `.analysis/pickup-20260917/user-run.log`:
- Ground marker frame 4167, capture 4170.
- Air marker frame 6760, capture 6763.
- Thrown pickups are registered, spatial support reports ready, and candidates
  exist. Selection stays zero with `outside gaze cone/range`; catch grips are seen.
- Ground capture shows the pistol below image center, outside the previous narrow
  eight-degree cone. Existing logs do not establish actual hand/weapon distances
  or whether the physical root is offset from the visible pistol.

## Changes

- Publish a fresh same-pawn headset ray from the rendered eye matrices. Previously
  gaze used the arm animation anchor, which omits late render yaw/pitch corrections.
  Solve the eye origins using all three clip planes to support asymmetric projection.
  This snapshot is used only for pickup gaze, not hand placement or camera rendering.
- Prefer validated, small world-space mesh bounds for the visible gun center;
  retain rigid-body and component/actor fallbacks when bounds are unavailable.
- Gaze cone widened from eight to fifteen degrees with a 12 cm minimum tolerance;
  retain the two-metre limit, line of sight, ammo and inventory eligibility checks.
- Catch radius increased from 18 to 25 cm, using the visible center and the existing
  swept-path test. Either tracked hand with its grip held can catch.
- Log weapon position source, actor position, view origin, gaze angle/distance,
  both hand distances, tracking flags, pose age and grip mask. Rate limited to two
  seconds, plus failed squeezes and an immediate report for each Backspace marker.

## Validation and installation

Passed production-function tests: `test-pickup-view.ps1`, `test-pickup-runtime.ps1`,
`test-combat-spatial.ps1`. Covers asymmetric projection, stale/wrong-pawn views,
matrix storage, bounds validation/fallbacks, both-hand catches, distance boundaries,
cooldown catches, tracking gates, gaze tolerance and occlusion.

Shipped-package metadata check passed all 28 interaction/spatial field types,
including PrimitiveComponent.Bounds. Runtime validation still resolves offsets.
Mandatory static analysis and x86 release build passed; existing analyzer warnings
remain. Headset behavior has not yet been verified for this build.

Installed `<game>\Binaries\d3d9.dll` SHA256:
`BD535A51A8455EBE8DBDC7844C974AEB07D6BC291C4E843207F19E06D4991BC2`.
Previous DLL backed up at `.analysis/pickup-20260917/d3d9-before.dll`, SHA256
`65F02E88FCA9570BDF3CAFDEA9D7799590ABA63118EF451435C689A6C3E0095C`.

Next headset check: look at a ground pistol within two metres, check for blue box,
squeeze either grip. Toss and catch with the other grip held. Mark failures with
Backspace while the gun remains visible, allowing the new position logs to identify
the remaining gate or coordinate mismatch.
